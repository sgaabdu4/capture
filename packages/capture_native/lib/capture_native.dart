/// The method channel is wrapped by the app (`lib/core/data/native/native_bridge.dart`);
/// this package only ships the macOS implementation.
library;

const captureNativeChannel = 'capture_native';
