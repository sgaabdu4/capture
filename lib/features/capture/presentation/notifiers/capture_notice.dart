/// One-line status the main window shows after a capture step.
enum CaptureNotice {
  setupIncomplete,
  micDenied,
  recordingNotStarted,
  tooShort,
  limitReached,
  recordingInterrupted,
  dismissed,
  reviewLater,
  saved,
  savedNotificationsOff,
  notionNotConnected,
}
